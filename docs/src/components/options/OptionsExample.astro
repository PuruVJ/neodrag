<div class="options-examples container">
	<div class="demo">
		<slot name="demo" />
	</div>

	<div class="code-sample">
		<slot name="codes" />
	</div>
</div>

<style lang="scss">
	.container {
		// For parent component
		scroll-snap-align: start;

		display: grid;
		grid-template-rows: 1fr auto;

		width: 600px;

		box-shadow: 0 0 0 1px hsla(var(--app-color-dark-hsl), 0.3);
		border-radius: 1rem;
	}

	.code-sample {
		display: grid;
		align-items: center;

		padding: 1rem;

		border-bottom-left-radius: inherit;
		border-bottom-right-radius: inherit;

		background-color: #0d1117;

		> :global(div) {
			display: grid;
			gap: 1rem;
		}
	}
</style>
