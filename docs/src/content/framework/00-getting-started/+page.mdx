---
title: 'Getting Started'
nav_title: 'Getting Started'
tagline: 'Install neodrag and make your first element draggable.'
---

import FrameworkSwitch from '$components/FrameworkSwitch.astro';

Neodrag is a small, composable, cross-framework, collab-first toolkit for **drag**, **drop**, **sortable**, **resize**, and **rotate** — one consistent API across Svelte, React, Vue, Solid, and vanilla JS.

## Install

Install your framework package **and `@neodrag/core`** — core is a peer dependency that holds the shared engine, so every capability resolves to one instance (it isn't auto-installed by pnpm). Your framework package's **root** is drag; the other capabilities live behind subpaths — `@neodrag/svelte/sortable`, `/resize`, `/rotate`, `/drop`, `/collab` — so you only pay for what you import.

<FrameworkSwitch>
<div slot="svelte">

```bash
npm i @neodrag/svelte@next @neodrag/core@next
```

Requires Svelte 5 (the wrapper uses attachments).

</div>
<div slot="react">

```bash
npm i @neodrag/react@next @neodrag/core@next
```

</div>
<div slot="vue">

```bash
npm i @neodrag/vue@next @neodrag/core@next
```

</div>
<div slot="solid">

```bash
npm i @neodrag/solid@next @neodrag/core@next
```

</div>
<div slot="vanilla">

```bash
npm i @neodrag/vanilla@next @neodrag/core@next
```

</div>
</FrameworkSwitch>

## Your first draggable

Each capability is a class (Svelte/vanilla) or a hook/primitive (React/Vue/Solid). Create it, attach it to an element, and read its reactive state.

<FrameworkSwitch>
<div slot="svelte">

```svelte
<script>
  import { Draggable } from '@neodrag/svelte';

  const drag = new Draggable({});
</script>

<div {...drag.attach}>
  {drag.isDragging ? 'Dragging…' : 'Drag me'}
</div>
```

</div>
<div slot="react">

```tsx
import { useDraggable } from '@neodrag/react';

function Box() {
  const { ref, isDragging } = useDraggable({});
  return <div ref={ref}>{isDragging ? 'Dragging…' : 'Drag me'}</div>;
}
```

</div>
<div slot="vue">

```vue
<script setup>
  import { useDraggable } from '@neodrag/vue';

  const { ref, isDragging } = useDraggable({});
</script>

<template>
  <div :ref="ref">{{ isDragging ? 'Dragging…' : 'Drag me' }}</div>
</template>
```

</div>
<div slot="solid">

```tsx
import { createDraggable } from '@neodrag/solid';

function Box() {
  const { ref, isDragging } = createDraggable({});
  return <div ref={ref}>{isDragging() ? 'Dragging…' : 'Drag me'}</div>;
}
```

</div>
<div slot="vanilla">

```js
import { Draggable } from '@neodrag/vanilla';

const node = document.querySelector('.box');
const drag = new Draggable(node, {});
// drag.update({ axis: 'y' }) to change options, drag.destroy() to clean up
```

</div>
</FrameworkSwitch>

That's a free-moving box. Pass options to shape it — `axis`, `bounds`, `grid`, `controls`, and more — all covered on the [Draggable](./draggable) page.

## The mental model

Four ideas carry the whole library:

- **Options** shape one interaction. `{ axis, bounds, grid, controls, … }` configure a single draggable, droppable, resizable, or sortable. Sensible built-ins like `touchAction` (touch drags aren't stolen as scroll) and `userSelect` (no text highlight while dragging) are always on.
- **Capabilities** are the four things you can do: **drag**, **drop**, **resize**, **sortable**. Each is its own class/hook with its own options, and they compose — a sortable item can also be droppable, a resizable panel can also be dragged.
- **Extras** extend an interaction through the `use: []` seam — `magnetic`, `scrollLock`, `autoScroll`, `ghost`, `haptics`, `ariaDrag`, and your own. They snap onto any capability without changing its core options.
- **Collab** is first-class. Every capability emits serializable ops on commit; a `Room` (from `@neodrag/core`) wires them to a backend — `room.add(target, id)` joins a draggable, resizable, or sortable under a shared id — for real-time multiplayer and live presence. Backends ship as `@neodrag/yjs` and `@neodrag/liveblocks`.

## Where to next

- [Draggable](./draggable) — the drag capability and every option.
- [Droppable](./droppable) — drop zones that auto-observe drags.
- [Resizable](./resizable) — resize handles, constraints, and aspect ratio.
- [Sortable](./sortable) — reorderable lists and grids with FLIP animation.
- [Extras](./extras) — the `use: []` extensions and how to author your own.
- [Collaboration](./collaboration) — real-time, multiplayer sortable.
- [Sensors](./sensors) — keyboard, gamepad, and pointer input.
- [Core](./core) — the `Interactions` engine for advanced and framework authors.
