import { Code } from 'astro/components';

## Installation

{/* prettier-ignore */}
<Code code={`
pnpm add @neodrag/${props.framework}

# or

npm install @neodrag/${props.framework}

# or

yarn add ${props.framework}
`.trim()} lang="sh" />
