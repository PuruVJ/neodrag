---
title: disabled
type: 'boolean'
defaultValue: 'false'
---

import Code from '$components/options/OptionsCode.astro';
import Example from '$components/options/OptionsExample.astro';
import Examples from '$components/options/OptionsExamples.svelte';

import DisabledExample from './Disabled.example.svelte';

export const shortDescription = 'Disables dragging';

{shortDescription}.

<Examples client:load>
  <Example>
    <DisabledExample client:visible slot="demo" />

    <Code slot="codes">
      <div slot="svelte">
        ```svelte
        <div use:draggable={{ disabled: true }}>
          Disabled. Won't drag, won't trigger any events
        </div>
        ```
      </div>

      <div slot="vue">
        ```vue
        <template>
          <div v-draggable="{{ disabled: true }}">
            Disabled. Won't drag, won't trigger any events
          </div>
        </template>
        ```
      </div>

      <div slot="solid">
        ```jsx
        <div use:draggable={{ disabled: true }}>
          Disabled. Won't drag, won't trigger any events
        </div>
        ```
      </div>

      <div slot="angular">
        ```html
        <div [neoDraggable]="{ disabled: true }">
          Disabled. Won't drag, won't trigger any events
        </div>
        ```
      </div>

      <div slot="react">
        ```ts
        useDraggable(draggableRef, { disabled: true });
        ```
      </div>

      <div slot="vanilla">
        ```js
        new Draggable(el, { disabled: true });
        ```
      </div>

    </Code>

  </Example>
</Examples>
