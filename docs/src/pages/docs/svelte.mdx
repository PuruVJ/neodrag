---
layout: '$layouts/MainDocsLayout.astro'
title: '@neodrag/svelte'
tagline: 'A lightweight Svelte Action to make your elements draggable.'
---

import Options from '$components/options/Options.astro';
import { version } from '../../../../packages/svelte/package.json';
import ExportedTypesMDX from '../../documentation/exported-types.mdx';

```sh
npm i @neodrag/svelte
```

## Usage

Basic usage

```svelte
<script>
  import { draggable } from '@neodrag/svelte';
</script>

<div use:draggable>Hello</div>
```

With plugins

```svelte
<script>
  import { draggable, axis, grid } from '@neodrag/svelte';
</script>

<div use:draggable={[axis('x'), grid([10, 10])]}>Hello</div>
```

Defining plugins elsewhere with typescript

```svelte
<script lang="ts">
  import { draggable, axis, bounds, BoundsFrom } from '@neodrag/svelte';
  import type { Plugin } from '@neodrag/svelte';

  let plugins: Plugin[] = [axis('y'), bounds(BoundsFrom.parent())];
</script>

<div use:draggable={plugins}>Hello</div>
```
